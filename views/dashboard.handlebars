<div class="container">
    <div class="blog-container">
        {{#each blogs as |blog|}}
        <div class="my-blog-card">
            <h1>{{blog.title}}</h1>
            <p>{{blog.date}}</h2>
        </div>
        {{/each}}
    </div>
</div>

